---
name: cli-template-toolkit
description: CLI application templates and patterns for Python. Use when building command-line tools with argparse, colored output, progress bars, logging, and error handling. Extracted from 12+ CLI tools via Ralph Loop analysis.
---

# CLI Template Toolkit

**Reusable CLI patterns extracted from Ralph Loop analysis of 12+ CLI tools.**

## What It Provides

Ready-to-use CLI patterns including:
- ✅ Standard argparse structure
- ✅ Colored console output
- ✅ Progress bars
- ✅ Logging setup
- ✅ Error handling
- ✅ Common utility functions

## Quick Start

### Basic CLI Template

```python
#!/usr/bin/env python3
import argparse
import sys
from pathlib import Path

def main():
    parser = argparse.ArgumentParser(description='Your CLI tool')
    parser.add_argument('input', help='Input file/directory')
    parser.add_argument('-o', '--output', help='Output file')
    parser.add_argument('-v', '--verbose', action='store_true', help='Verbose output')
    
    args = parser.parse_args()
    
    # Your logic here
    print(f"Processing: {args.input}")
    
if __name__ == '__main__':
    main()
```

### Colored Output Template

```python
class Colors:
    OK = '\033[92m'
    WARN = '\033[93m'
    ERROR = '\033[91m'
    INFO = '\033[94m'
    END = '\033[0m'

def log_ok(msg): print(f"{Colors.OK}[OK]{Colors.END} {msg}")
def log_warn(msg): print(f"{Colors.WARN}[WARN]{Colors.END} {msg}")
def log_error(msg): print(f"{Colors.ERROR}[ERR]{Colors.END} {msg}")
def log_info(msg): print(f"{Colors.INFO}[INFO]{Colors.END} {msg}")
```

### Progress Bar Template

```python
import sys

def progress_bar(current, total, bar_length=50):
    fraction = current / total
    arrow = int(fraction * bar_length - 1) * '-' + '>'
    padding = int(bar_length - len(arrow)) * ' '
    ending = '\n' if current == total else '\r'
    print(f'Progress: [{arrow}{padding}] {int(fraction*100)}%', end=ending)
```

## Scripts

Use bundled scripts for common CLI tasks:
- `scripts/argparse_boilerplate.py` - Full argparse template
- `scripts/colored_logger.py` - Color-coded logging
- `scripts/progress_utils.py` - Progress bar utilities

## Usage Examples

"Create a CLI tool that processes CSV files with progress bar and colored output"

→ Use this skill to generate the boilerplate instantly.

## Ralph Loop Origin

This skill was auto-generated by Ralph Loop from patterns found in:
- tempest-weather-skill
- github-skill-publisher
- skill-factory
- sharepoint-orphan-finder
- adobe-sign-cache-reset
- And 7 more CLI tools...
